Setup using Linux:

1) Install nodejs, npm and express
sudo apt-get update
sudo apt-get install nodejs
sudo pat-get install npm
sudo npm install express --save

2) Launch server on localhost
node server.js

3) View the webpage in browser at: http://localhost:5000
